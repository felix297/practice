# 处理新的一课的单词
1. 在 `rawFile` 添加前两列数据：罗马字 和 是否需要汉字到假名
   - 假名后面接汉字，汉字前面加空格
   - 英文当作汉字处理
1. 检查音频文件是否搭配: 使音频文件名 = 生成数据中音频文件名
   - 音频文件数量 > `rawFile` 中单词数量：在 `rawFile` 造一个假数据
   - 音频文件数量 < `rawFile` 中单词数量：手动寻找缺失音频，将 `rawFile` 中该单词移到最后
1. 给汉字加上 振りがな